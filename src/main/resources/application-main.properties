# ==============================================================================
# Production / Main Profile Configuration
# ==============================================================================

server.port=${SERVER_PORT:8080}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT_PATH:/api/dms}

# Config Server
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://nnp-config-service:8080}/nnp-config

# Production Logging
logging.level.com.nnp.dms=INFO
logging.level.org.springframework.web=WARN
